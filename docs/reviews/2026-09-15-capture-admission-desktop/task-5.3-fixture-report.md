# Task 5.3 existing consumer fixture migration

Main-authored test-only changes in the separate native copy, six paths. No production gate was changed in this slice. Independent fixture review is pending; the production owner's 46-file review is separate.

Coordinator/native/operational tests now use currentCaptureFixture, which calls the actual closed reader, bounded snapshot continuation and coherent collector with a controlled Figma API and valid generated PNG bytes. Direct coordinator/native invocations carry its explicit Chrome URL-scope grant. Old pinned JSON stand-ins in native/operational positive fixtures are replaced by signed owner-state captures; unrelated project fixture files remain source data. Component test stubs now explicitly provide empty component API definitions and valid NAVIGATE action metadata where those API observations were intended. Catalog values are supplied to the closed reader and the public paging regression remains intact.

The source-authority and IR completion unit predicates still use explicit synthetic unrelated admission/receipt fields to isolate their intended checks; comments distinguish these from runtime proof. Source closure gets a real controlled capture descriptor. The IR predicate gets a structurally valid synthetic descriptor/admission/receipt and additionally checks missing capture receipts reject. Existing source-byte/legacy/operational/authorization assertions remain.

The suspended freshness test is now parameterized for unchanged and changed applied targets. It starts with a require-live plan, creates a new coherent capture whose evidence hash differs but semantic fingerprint matches, exercises an admitted actual native validation, checks current original/fresh receipt identity on the unchanged target, and preserves the final applied-source rejection on the changed target. Both cases verify the original signed raw capture and assetRoot remain unchanged. These are controlled capture ports around real native work, not live Figma.

Validation: main six-file suite passed 71 tests, exit 0, 116.33 seconds; includes 31 coordinator, 30 native-work, 3 capture-flow, 2 operational HTTP/SQLite, 4 source-authority and 1 IR-completion tests. Earlier smoke checks preserved 28/29 native tests before replacing the old duplicate-design-write freshness fixture; that failed with EEXIST because current plans already publish the immutable capture. The corrected freshness positive/race and capture-flow subset passed all five selected tests. Final full MCP and IR TypeScript checks pass. All six owned files pass formatter and lint with deny-warnings.

The separate copy shares the Windows account and filesystem/process/network privileges, not an OS sandbox. No Docker, Superpowers, browser, daemon, build, dependency, Git index or commit action. Exact hashes/main preimages are in task-5.3-fixture-owned-files.json. The worker-owned capture-fixture.ts and production contracts are covered by the main Task5.3 source review, not by this manifest.
