# Task 5.5 implementation report

Status: implementation frozen for independent review. The current 19-test scoped run passed; a final seven-test repetition for the last source visibility refinement is running. No source was integrated into the main working tree, staged, committed, or pushed by this worker. The controller must review and verify the final owned-file manifest before integration.

## Scope and authority

All implementation and native validation use `C:/Users/c/AppData/Local/SuperFigmaPipeline/validation/remaining-20260915/service`. No Superpowers skill, Docker, VM, WSL, live user Chrome/Figma session, daemon, primary distribution build, dependency installation, or Git mutation was used. Local test HTTP servers and headless Firefox instances are owned fixtures. This remains execution under the same Windows account with normal filesystem, network and process privileges, not an OS sandbox.

The initial assigned source paths were expanded with controller approval to the existing atomic directory lease invocation descriptor and native artifact/module capability code. This expansion was required by an actual worker failure, not hypothetical hardening: the existing retained-directory writer launches its fixed PowerShell lease broker, which the native module fence correctly rejected as unbound until that exact helper was admitted.

## Implemented consuming path

- The current captured design and workflow coverage produce a versioned source-linked interaction contract. Source reaction identity, node/root/destination, raw source hash, trigger, action, required workflow association and captured motion properties are retained. Missing interaction capability, unknown actions/triggers, uncaptured destinations, unsupported easing/motion and limits remain blocked.
- The coordinator includes this contract in context and blueprint hashes and exposes it in the actual next-step instruction. Rendered source roots use `data-sfp-root`; required controls and state observations use exact `data-sfp-node` markers. Historical plans without the contract remain inspectable but cannot execute as current plans.
- Owner profile preparation creates an expected observation manifest before approval. Every captured PNG root has one explicit screen or the existing narrow primitive-copy exception. Source root, capture fingerprint, scope hash, route, source state, viewport, scale, oracle and mandatory assertion IDs are bound. Equal PNG hashes never combine root obligations.
- Prepared native preview commands use a fixed service bootstrap and exact approved original Node server arguments. The existing validator bundle and browser runtime remain inventory-bound. The root worker launches the actual app server and Firefox under the existing Windows Job; normal and failure paths still require module/output proof and the durable Job stop receipt.
- An ephemeral authenticated pipe delivers the approved worker configuration and returns a bounded structured receipt. The worker removes its pipe and token from its environment before importing the validator or launching children. Candidate stdout is excluded from accepted visual proof. Missing, wrong-token, duplicate, truncated and differently bound receipts fail.
- The worker verifies the actual server child's listening endpoint before and after browser assertions using fixed, bound IPv4/IPv6 `netstat` read invocations. The child must remain alive. An unrelated existing listener cannot satisfy readiness or visual acceptance. Only the approved endpoint, server PID, protocol and check count are retained; machine-wide query output is not published.
- The worker's actual Firefox reports flow through `NativePortalRunner` into `NativeWork`, which independently verifies saved PNG bytes, original source files, exact observation identities and complete required action/workflow coverage. Native visual, interaction and accessibility claims require the authenticated worker path; a successful command or copied stdout label cannot satisfy them. IR completion additionally requires the current interaction/observation receipt.

## Interaction behavior

Captured navigation, overlays, close, back, variant/state changes and scrolling have concrete browser assertions. Source elements must exist uniquely and be visible. New overlay/state targets must not already be visible; state replacement must hide the old source. Required source reactions execute even when optional caller action arrays are empty.

Required basic form/workflow assertions use source-linked selectors and require both a mutating action and an observed changed outcome. Clicking an unused control followed by an unchanged decorative text/visibility assertion is insufficient. Read-only workflows do not acquire fabricated mutation requirements merely because their blueprint is confirmed.

Still images retain reduced motion, fixed time, scale 1, exact dimensions and the 3% maximum difference threshold. Required motion executes separately with reduced motion disabled. Its proof includes observed visible animation samples and elapsed completion time, with captured simple easing/direction constraints when present. Static PNG equality cannot satisfy temporal coverage. Supported motion observation is bounded to 10 seconds and same-document browser animations; unsupported custom easing, unknown motion, unresolved variables/conditional actions, and unobserved transitions do not become passing evidence.

Basic accessibility checks are retained and missing image/font/script/stylesheet requests and broken DOM images are now explicit failures.

## Budgets and integration contract

- At most 16 screens per worker, 256 screens across the prepared manifest, 32 native commands, 128 primitive assets and 4,096 source interaction assertions. Disjoint commands use `.sfp-native-preview/<command-id>` outputs, avoiding overlapping producer receipts. The supplied historical nine screens plus two primitive assets fit one worker's count limits; live current-source acceptance is still the controller's Task 7 work.
- Worker configuration and receipt are bounded to 4 MiB each. Structured preview receipts do not consume the candidate stdout budget. Combined verifier input is bounded to 16 MiB, each PNG to 16 MiB, and captured/actual image reads to 512 MiB per reader.
- Each host listener read has a 5-second timeout and 1 MiB output bound. The preview itself has a 5-minute ceiling and is further bounded by its approved native command deadline.
- Native preview currently requires a direct Node server listener owned by the worker's actual child process. An npm wrapper whose different child owns the socket is not assumed to be that server; select its direct Node entry. Windows native process proof remains the supported host path.
- Raw standalone `assertNativePortalPreview` remains a useful diagnostic API, but its stdout and historical reports do not establish current native acceptance.
- The two new native child capabilities are exact fixed directory-lease PowerShell and exact read-only `netstat` calls. Different scripts, flags, executables and generic shell access remain denied. Existing generic filesystem and process protections were not relaxed.

## Verification evidence

Earlier focused runs established:

- Current-capture/observation tests: distinct equal-byte roots, missing/duplicate report, wrong root/route/state/viewport/assertion, captured navigation with empty optional actions, hidden-control rejection, and legacy/tamper/dangling destination checks.
- Actual native worker positive through the profile preparation and `NativeWork` consumer, followed by forged stdout/no-worker and unrelated-listener negatives. The worker test builds the current validator into a unique task-owned cache directory each run; it never uses stale primary distribution output.
- Actual local IPv4, IPv6 and localhost listener ownership positives and wrong-PID negatives.
- Actual form-state positive and unchanged outcome negative; measured 300 ms opacity motion (observed approximately 296-321 ms), static-only negative, and real close/change-state/back positives.
- Exact lease helper negative variants and the existing generic module-fence suite.
- Compatibility sweep: 83 passed and one synthetic IR receipt fixture needed migration. That fixture was corrected; no production check was loosened.

A broader 61-test run retained at `.cache/task55-final-tests.log` had 59 passed, one existing opt-in skip and one failure. The existing `native-artifacts` add-mutation test received a failed command with exit code 2148732969 and empty output, rather than executing its intended mutation. This did not produce false acceptance. Its exact isolated rerun passed (one selected test, thirteen filtered tests). The original host startup failure is not claimed to have an established cause.

The final current-source results and hashes are recorded below after completion. MCP typechecking has also temporarily reported concurrent changes outside this task in `recipe-evidence-hold.test.ts` and `egress-finalizer.test.ts`; the controller was notified. This worker does not modify those files.

## Remaining controller work

Perform independent scoped review and reproduce any accepted finding, integrate exact verified bytes, update generated contract/provenance metadata when shared contracts settle, adapt the final C4/C2/C3 profiles and generated source to the new source markers and typed preview commands, then run the already-planned final whole-code review rounds and release verification. This task does not claim live Figma acceptance, complete core recipe consumption, full portal business behavior, final repository verification, or Git delivery.

The task-owned cache bundles, local diagnostic build config and logs are excluded from source integration. Original captured asset roots are never rewritten by preview.

## Frozen source manifest

| Path | SHA-256 |
| --- | --- |
| packages/shared/src/portal-observations.ts | e1583fb821e2797b739511eb5ff3ea6269c814ffcb7748bf5cd6a4ab4b959d1d |
| packages/shared/src/portal.ts | a0ca15a2c86acd649571269ca5381d62e49eff62f1b0e7dca8b2de08f9cb809a |
| packages/ir/src/portal-run.ts | 4baf150deb2907da8cce40070128a5448fd5beb98d33dac1491c0df249dfe325 |
| packages/ir/test/portal-completion.test.ts | 72686c25ad93eb6aac8f8af654be85c266fac87c5e1e81e0ab8b15804ba364b3 |
| packages/mcp/src/portal/interaction-evidence.ts | 2e276b862d8d0a0d4e0add678a3691562408307084158a4cd712dd88aff65ab6 |
